[project]
name = "amazon-tracker-mvp"
version = "0.1.0"
description = "Amazon product tracker MVP — scrapes AE, SA, AU marketplaces and stores time-series snapshots"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
